name: Maintainer Permissions Review

on:
  schedule:
    # Run annually on January 1st at 10:00 AM UTC
    - cron: '0 10 1 1 *'

  # Allow manual triggering for testing
  workflow_dispatch:

jobs:
  review-maintainer-permissions:
    uses: otterdog-kairoaraujo/otterdog-configs/.github/workflows/project-permissions-review.yml@main
    with:
      frequency_months: 12
